<% content_for :title, "Edit password" %>
<nav class="navbar navbar-static-top" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a href="../" class="navbar-brand">Codebattalion</a>
          </div>
          <div id="navbar" class="navbar-collapse collapse" aria-expanded="false" style="height: 1px;">
          	<% if logged_in? %>
          	<a href="../profile/<%= current_user.username %>" class="navbar-brand" style="float:right"><%= current_user.username %></a>
          	<% end %>
	        <ul class="nav navbar-nav">
	        <li>
	          <a href="../problems">Problems</a>
	        </li>
	        <li class="active">
	          <a href="#">Leaderboard</a>
	        </li>
          <% if logged_in? %>
          <li>
	          <a href="../submissions/<%= current_user.id%>">Submissions</a>
	        </li>
	        <li>
	          <a href="../logout">Logout</a>
	        </li>
          <% end %>
	      </ul>
            
          </div><!--/.nav-collapse -->
        </div><!--/.container-fluid -->
      </nav>
<div class = "container">
	<% if current_user.errors.any? %>
	<div id="error_explanation">
	    <% current_user.errors.full_messages.each do |msg| %>
  			      <li><%= msg %></li>
  			    <% end %>
  			    </ul>
  		</div>
  	<% end %>
    <%= form_for :passdit, url: {action: "editpass"} do |f| %>
    <div class="form-group">
        <label for="InputOldpass">Old password</label><br/>
        <input type="password" class="form-control login-field" name = "oldpass" required>
    </div>
    <div class="form-group">
        <label for="InputNewpass">New Password</label><br/>
        <input type="password" class="form-control login-field" name = "newpass" required>
    </div>
    <div class="form-group">
        <label for="InputConfpass">Confirm Password</label><br/>
        <input type="password" class="form-control login-field" name = "confirmpass" required>
    </div>
    <%= f.submit "Save Password", class: "btn btn-primary btn-large"%>
    <% end %>
</div>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>